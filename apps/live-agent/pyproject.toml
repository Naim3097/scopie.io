[project]
name = "scopie-live-agent"
version = "0.1.0"
description = "Scopie Live host brain — LiveKit Agents worker that answers viewer chat and drives the avatar."
requires-python = ">=3.11"
dependencies = [
    "livekit-agents>=1.0",
    # Avatar renderer plugin — HeyGen LiveAvatar via the LiveKit avatar interface.
    # Swappable for anam / tavus / simli plugins without touching agent logic.
    # "livekit-plugins-heygen",
    # STT/TTS for BM/EN (benchmark against mesolitica Malaysian models):
    # "livekit-plugins-deepgram",
    # "livekit-plugins-elevenlabs",
    "anthropic>=0.40",
    "httpx>=0.27",
]

[tool.uv]
dev-dependencies = ["ruff>=0.6"]
