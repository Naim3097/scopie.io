{
  "name": "@scopie/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Scopie background workers — event aggregation, payment reconciliation, moderation jobs, notifications.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@scopie/core": "workspace:*",
    "bullmq": "^5.34.0",
    "dotenv": "^16.4.0",
    "ioredis": "^5.4.0",
    "pg": "^8.13.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
